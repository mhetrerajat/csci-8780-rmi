/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package rmiproject.app;

import rmiproject.list.LinkedList;

import static rmiproject.utilities.StringUtils.join;
import static rmiproject.utilities.StringUtils.split;
import static rmiproject.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
